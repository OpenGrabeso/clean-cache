{
  "name": "@opengrabeso/clear-cache-action",
  "description": "Cleans up caches for GitHub Actions by their keys or branches. ",
  "author": "Ondrej Spanel <ondrejspanel@users.noreply.github.com>",
  "license": "MIT",
  "readme": "README.md",
  "type": "module",
  "main": "dist/index.cjs",
  "scripts": {
    "build": "esbuild index.js post.js --bundle --format=cjs --platform=node --target=node24 --outdir=dist --out-extension:.js=.cjs"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.1.1",
    "@octokit/rest": "^22.0.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "esbuild": "^0.28.0"
  }
}
